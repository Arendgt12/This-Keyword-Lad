# JS Context Lab: Mastering `this`
# Lab done by :[Jael Johnson]
# CMPS2212

A collection of four exercises exploring how JavaScript handles context (`this`) in the DOM and within objects.

## 📂 Exercises

1. **Multi-Button Highlight** (`exercise1.js`): Use one function and `this` to style multiple buttons via a `forEach` loop.
2. **Data Attribute Reader** (`exercise2.js`): Fetch values from HTML using `this.dataset` to dynamically change backgrounds.
3. **Object Context** (`exercise3.js`): Use `.bind()` to keep a method's `this` pointed at an object instead of a button.
4. **The Arrow Function Bug** (`exercise4.js`): Fix scoping issues by using `event.currentTarget` instead of `this` inside arrow functions.
